[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# `pytest` alone does not add the working directory to sys.path; this makes the
# `app` package importable regardless of how the suite is invoked.
pythonpath = .
